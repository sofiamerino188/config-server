server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sofiamerino188/bootcamp-configServer.git
        default-label: main
        svn:
          search-paths:
          - '*service*'
